procs:
  ngrok:
    cmd: ["npm", "run", "ngrok:dev"]

  inngest:
    cmd: ["npm", "run", "inngest:dev"]

  next:
    cmd: ["npm", "run", "dev"]

  market-listener:
    cmd: ["npx", "dotenv-cli", "--", "npx", "tsx", "services/market-listener/index.ts"]
